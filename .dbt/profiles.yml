eth:
  outputs:
    ci:
      account: SKRQUHQ-VJ39361
      database: CI
      private_key_path: "{{ env_var('DBT_PRIVATE_KEY_PATH') }}"
      role: ACCOUNTADMIN
      schema: CI_SCHEMA
      threads: 1
      type: snowflake
      user: Lwazi
      warehouse: compute_wh
  target: dev